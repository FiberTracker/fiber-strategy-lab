(()=>{'use strict';const base=document.body.dataset.base||'./',page=document.body.dataset.page||'home';const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));const pct=x=>x==null?'Not available':(100*x).toFixed(1)+'%';const num=x=>x==null?'Not available':Math.round(x).toLocaleString('en-US');const section=(id,kicker,title,body)=>`<section class="section deep-section" id="${id}"><div class="section-head"><div><p class="section-kicker">${kicker}</p><h2>${title}</h2></div></div>${body}</section>`;
async function mount(){const main=document.querySelector('main#main');if(!main||main.dataset.deepLoaded)return;main.dataset.deepLoaded='true';try{const[d,lib]=await Promise.all([fetch(base+'content.json').then(r=>r.json()),fetch(base+'analysis-library.json').then(r=>r.json())]);let h='';if(page==='omni'&&d.omni.carrierOverlap){const o=d.omni.carrierOverlap;h+=section('carrier-overlap','Exact location intersection','Test incremental reach carrier by carrier',`<p>${esc(o.method)}</p><p class="deep-note"><strong>Scope:</strong> ${esc(o.target)} ${esc(o.limit)}</p><div class="deep-table-wrap"><table class="deep-table"><thead><tr><th>State</th><th>Comparator</th><th>Omni locations</th><th>Shared locations</th><th>Share overlapping</th><th>Outside selected comparator</th></tr></thead><tbody>${o.rows.map(r=>`<tr><th>${esc(r.state)}</th><td>${esc(r.comparator)}<small>${esc(r.status)}</small></td><td>${num(r.targetLocations)}</td><td>${num(r.intersection)}</td><td>${Number(r.overlapPct).toFixed(1)}%</td><td>${num(r.outsideComparator)}</td></tr>`).join('')}</tbody></table></div><p class="deep-note">Availability as of December31,2025. Outside a selected comparator is not a measure of market demand or lack of competition. <a href="https://broadbandmap.fcc.gov/data-download">FCC source</a></p>`);}if(page==='omni'&&d.omni.measuredContext){const m=d.omni.measuredContext;h+=section('measured-context','Measured geographic context','What the reported locations say about place',`<p>${esc(m.methodology)}</p><div class="deep-table-wrap"><table class="deep-table"><thead><tr><th>Historical filer / market</th><th>FCC R/X locations</th><th>Income context, 2024 dollars*</th><th>Income-valid locations</th><th>5+ unit structure context*</th></tr></thead><tbody>${m.rows.map(r=>`<tr><th>${esc(r.label)}<small>Filer ${r.providerId}; ${r.blockGroups} block groups</small></th><td>${num(r.locations)}</td><td>$${num(r.metrics.median_household_income_2024_dollars.weighted_context_average)}</td><td>${pct(r.metrics.median_household_income_2024_dollars.valid_share)}<small>${num(r.metrics.median_household_income_2024_dollars.valid_locations)} locations</small></td><td>${pct(r.metrics.share_units_in_structures_5_plus.weighted_context_average)}</td></tr>`).join('')}</tbody></table></div><p class="deep-note">*Availability-weighted average of ACS block-group median household incomes or housing shares. These are geographic context measures, not customer incomes or an operator MDU-unit share. Every row has a 100% geographic join; missing and open-ended income estimates reduce the valid-location coverage shown separately.</p><div class="income-bands">${m.rows.map(r=>`<div class="income-band-row"><strong>${esc(r.label)}</strong><div class="income-band-bar" role="img" aria-label="${esc(r.incomeContextDistribution.map(b=>b.label+': '+pct(b.share_of_valid)).join('; '))}">${r.incomeContextDistribution.map((b,i)=>`<span class="band-${i}" style="width:${100*b.share_of_valid}%" title="${esc(b.label)}: ${pct(b.share_of_valid)}">${b.share_of_valid>.12?pct(b.share_of_valid):''}</span>`).join('')}</div></div>`).join('')}<p class="deep-note">Local income context bands: <span class="band-key band-0"></span> Below $60k <span class="band-key band-1"></span> $60k–$90k <span class="band-key band-2"></span> $90k–$120k <span class="band-key band-3"></span> $120k+. Shares use locations with valid income estimates.</p></div><details><summary>Method, uncertainty and perimeter</summary><p>${esc(m.interpretation)}</p><p>${esc(m.perimeter)}</p><p>${esc(m.geography)}</p><p>${esc(m.uncertainty)}</p><p><a href="https://www.census.gov/programs-surveys/acs/news/data-releases/2024/release.html">ACS 2024 five-year release</a> · <a href="https://broadbandmap.fcc.gov/data-download">FCC downloads</a></p></details>`);h+=calculator();}
if(page==='rightfiber'&&d.rightfiber.exactOverlap){const o=d.rightfiber.exactOverlap;h+=section('southern-exact-overlap','Southern reporting footprint',esc(o.title),`<p>${esc(o.summary)}</p><p class="deep-note">${esc(o.scope)}</p><a class="button" href="${esc(base+o.url)}">Explore exact overlap and county audit</a>`);}
const analysisPage=page==='states'?d.atlas:d[page];if(analysisPage&&analysisPage.acceleratedAnalyses){for(const o of analysisPage.acceleratedAnalyses){h+=section('accelerated-diligence-'+o.url.split('/')[1],'Detailed diligence',esc(o.title),`<p>${esc(o.summary)}</p><p class="deep-note">${esc(o.scope)}</p><a class="button" href="${esc(base+o.url)}">Open the detailed analysis</a>`);}}
const relevant=lib.filter(x=>page==='home'||x.pages.includes(page));if(relevant.length)h+=section('full-analysis','Research library','Read the full analysis',`<p>Detailed carrier cases, transaction structures and sourced operator dossiers.</p><div class="library-grid">${relevant.map(x=>`<a class="library-link" href="${esc(base+x.url)}"><small>${esc(x.type)}</small><strong>${esc(x.title)}</strong><span>${x.operators?x.operators+' operator profiles':num(x.words)+' words'} ↗</span></a>`).join('')}</div>`);main.insertAdjacentHTML('beforeend',h);if(page==='omni')bindCalculator();}catch(e){console.error('Detailed analysis failed to load',e);const p=document.createElement('p');p.textContent='Detailed analysis could not load. Please reload this page.';main.append(p);}}
function calculator(){const fields=[['units','Serviceable units','number','1'],['penetration','Retail penetration (%)','number','0.1'],['arpu','Net monthly service revenue per subscriber ($)','number','0.01'],['variable','Monthly variable cost per subscriber ($)','number','0.01'],['fixed','Monthly property operating cost ($)','number','0.01'],['share','Flat property revenue share (%)','number','0.1'],['capex','Net initial capital cost ($)','number','1'],['maintenance','Annual maintenance capital ($)','number','1']];return section('mdu-worksheet','Your assumptions, not company metrics','Test a retail MDU property',`<p>Enter a property scenario to estimate recurring contribution and simple capital recovery. No Omni pricing, take rate or capital cost is assumed.</p><form id="mdu-form"><div class="worksheet-grid">${fields.map(([id,label,type,step])=>`<label>${label}<input id="mdu-${id}" type="${type}" min="0" ${['penetration','share'].includes(id)?'max="100"':''} step="${step}" required autocomplete="off"></label>`).join('')}</div><button class="button" type="submit">Calculate property scenario</button> <button class="button secondary" type="reset">Clear</button></form><div id="mdu-results" class="worksheet-results" aria-live="polite"><p>Complete all fields to calculate. Enter zero only where zero is your actual assumption.</p></div><details><summary>Calculation and scope</summary><p>Annual recurring contribution = 12 × serviceable units × penetration × [net monthly revenue × (1 − property share) − monthly variable cost] − 12 × fixed monthly property cost.</p><p>Cash contribution subtracts annual maintenance capital. Simple payback divides initial capital by positive annual cash contribution. Cash break-even penetration covers recurring property cost and maintenance, before recovering initial capital.</p><p>This worksheet excludes central overhead, taxes, financing, growth capex and uncontracted wireless revenue. It does not estimate company EBITDA or valuation. Property rights, capital allocation and permitted consideration must be verified against the executed agreement.</p><a href="${base}research/mdu-underwriting/">Full MDU underwriting framework</a></details>`);}
function bindCalculator(){const form=document.getElementById('mdu-form'),out=document.getElementById('mdu-results');if(!form)return;form.addEventListener('submit',e=>{e.preventDefault();const n=id=>Number(document.getElementById('mdu-'+id).value),u=n('units'),p=n('penetration')/100,a=n('arpu'),v=n('variable'),f=n('fixed'),s=n('share')/100,c=n('capex'),m=n('maintenance');if(u<=0){out.textContent='Serviceable units must be greater than zero.';return;}const unit=a*(1-s)-v,con=12*u*p*unit-12*f,cash=con-m,be=unit>0?(12*f+m)/(12*u*unit):null;out.innerHTML=`<div class="worksheet-grid"><div><small>Annual property contribution</small><strong>$${num(con)}</strong></div><div><small>After maintenance capital</small><strong>$${num(cash)}</strong></div><div><small>Simple capital payback</small><strong>${cash>0?(c/cash).toFixed(1)+' years':'Not recovered'}</strong></div><div><small>Cash break-even penetration</small><strong>${be==null?'Not achievable':pct(be)+(be>1?' (above capacity)':'')}</strong></div></div><p class="deep-note">Calculated from your inputs. Constant annual run rate; no ramp, tax, financing or central overhead modeled.</p>`;});form.addEventListener('reset',()=>out.innerHTML='<p>Complete all fields to calculate. Enter zero only where zero is your actual assumption.</p>');}
const observer=new MutationObserver(()=>{if(document.querySelector('main#main')){observer.disconnect();mount();}});if(document.querySelector('main#main'))mount();else observer.observe(document.getElementById('app')||document.body,{childList:true,subtree:true});})();
